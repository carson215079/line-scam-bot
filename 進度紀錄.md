# 進度紀錄 — 防詐小幫手 LINE Bot

> 本檔案記錄專案所有工作進度。每次完成工作必須更新此檔。
> 規則定義見 `CLAUDE.md`「本專案工作守則」。

**最後更新：2026-08-26**

---

## 階段六：根治推播重複 + 改用台灣媒體 RSS（2026-07-19）

**問題**：每日推播內容一直重複。根因是先前加的「推播只挑已解碼文章」安全網，
配上 Render 無法解碼 Google 連結（新文章都是未解碼狀態）→ 推播永遠卡在
07/15 那批手動修好的舊文章。屬「修法有意外副作用」（見經驗教訓）。

**根治**（用戶選擇「改用台灣媒體 RSS」）：
1. 爬蟲改用台灣媒體 RSS（中央社/自由/ETtoday），用關鍵字過濾詐騙新聞。
   來源直接提供真實網址，**徹底擺脫 Google 解碼依賴**（Render 解碼問題根除）。
   - 移除 crawler/source_165.py、source_news.py，改為 base.py 內
     RssKeywordCrawler + TAIWAN_FEEDS + SCAM_KEYWORDS
2. 推播去重：articles 新增 broadcasted_at 欄位；推播只取未推播文章，
   發送成功才標記已推（失敗不標記，避免漏推）。
3. 資料收尾：現有 201 篇舊文標記為已推播（避免再推舊的）；本機跑一次爬蟲
   種入 9 篇新鮮台灣新聞（真實網址）供隔日推播。
4. 順手修正過時測試（summarize_article 已回傳 dict、無結果改 AI 回答）；
   database 測試改用測試前綴網址並自動清理，避免污染正式 Supabase。

**驗收點**：2026-07-20 早上 9 點推播應為全新台灣媒體新聞、連結手機可點、不重複。

---

## 階段七：修正非詐騙新聞誤收 + 排程改外部觸發（2026-07-22）

**問題A：早上 9 點沒推播**
根因：Render 免費方案休眠 → 行程內的 APScheduler 一併停擺（20 小時沒爬文）。
證據：健康檢查回應耗時 41.7 秒（冷啟動特徵）、程式本身無錯誤。
**對策**：新增外部觸發端點 `/tasks/crawl`、`/tasks/broadcast`、`/tasks/cleanup`
（皆需 ADMIN_KEY），由 cron-job.org 定時呼叫，可同時喚醒服務並確實執行任務。
與 APScheduler 並存不會重複執行（URL 唯一 + broadcasted_at 去重）。

**問題B：推播出現非詐騙新聞（詐領助理費、詐領健保、RDX 火藥）**
根因有二：
1. 關鍵字比對 `description`，而該欄含 HTML 圖片網址 —— ETtoday 圖檔名
   `c8838165.jpg` 含「165」被誤判命中。
2. 使用「詐」「騙」單字關鍵字 → 誤中「詐領」（公務貪瀆，非民眾受害詐騙）。
**對策**：
- 改為**僅比對標題**，不碰 description
- 關鍵字改用完整詞彙（詐騙/詐欺/詐團/假檢警/解除分期…），移除單字與裸數字 165
- 新增排除詞（詐領/助理費/貪污/侵占/浮報/溢領）
- 抽出 `is_scam_article()` 並加 9 個測試（含圖檔名誤判的回歸測試）
- 清理待推播佇列中 5 篇不符文章，剩 9 篇全為真詐騙案

---

## 階段八：推播新聞去重（2026-07-26）

**問題**：推播出現內容雷同的新聞（同事件不同報導）。原 `broadcasted_at` 機制
只能防「同一篇被推兩次」，擋不掉「不同 URL 但內容雷同」。

**修正**：
- `title_similarity()`：字元 bigram Jaccard 相似度（實測不同新聞<0.2、同案~0.85）
- `get_articles_for_broadcast()`：撈 30 候選，過濾掉與「近 7 天已推播」或
  「本批已選」相似度 ≥0.45 者，再取 5 篇 → 跨天＋同批都不重複
- 爬蟲存入前加 `title_exists()` 擋同標題重複（省 API）
- 清理現有 1 組同標題重複 + 61 篇殘留 Google 舊連結
- 加 2 個相似度測試（共 31 測試通過）

**能力範圍（誠實）**：標題相似度可抓「同事件、用詞雷同」的重複；若兩篇報導
同事件但用詞差異大（如標題相似度 <0.45），仍可能都被選中。要更精準需 AI
語意比對，但成本較高且有誤刪不同新聞的風險，暫不採用。

**同日好消息**：07/26 09:00 推播已由 cron-job 自動觸發成功（保活修正生效）。

---

## 階段九：專案體檢與清理（2026-07-26）

**修正的問題：**
1. **雙重推播 race condition**：APScheduler 與 cron-job 都在 9:00 觸發推播，若同時跑
   會各抓 5 篇 → 用戶一早收到 10 篇。加 `has_broadcast_within(12h)` 守衛，一天只推一次。
2. **對話歷史可能以 assistant 開頭**：Anthropic API 要求首則為 user，孤兒 assistant
   訊息會導致 API 報錯。`get_conversation_history` 去掉開頭多餘的 assistant。
3. **System prompt 矛盾**：圖片分析重用 SYSTEM_PROMPT（強制「詐騙手法/如何防範」格式），
   但圖片提問要「【判斷】【說明】【防範】」格式 → 指令打架。改用中性 IMAGE_SYSTEM_PROMPT。

**清掉的多餘程式碼：**
- `resolve_url`（50 行 Google News 解碼）— 改用台灣 RSS 後主程式完全沒用到
- `get_latest_articles` — 推播改用 get_articles_for_broadcast 後成為死程式碼
- `fetch_article_text` 的 Google News 死分支
- 刪 `scripts/fix_urls.py`（DB 已無 Google 連結，過時）
- 簡化 `scripts/remigrate_articles.py`（移除 Google URL 修復，保留重寫摘要）

共減少 143 行、新增 47 行，31 測試全過。

---

## 階段十：GitHub Actions 排程 — 方案A（2026-08-26）

**問題**：08/18 之後連續 8 天沒有推播。根因不變——cron-job.org 再次失效，
Render 休眠 → APScheduler 停擺。同一問題已發生 4-5 次，cron-job.org 治標不治本。

**修正（方案A — GitHub Actions）**：
把排程從 Render（會睡著）搬到 GitHub Actions（不會睡著、免費）。

新增檔案：
- `.github/workflows/daily.yml` — 三個 cron 排程：
  - 每 3 小時爬取（`0 */3 * * *`）
  - 每天 09:00 台灣時間推播（`0 1 * * *` UTC）
  - 每天 04:00 台灣時間清理（`0 20 * * *` UTC）
  - 支援手動觸發（workflow_dispatch）
- `scripts/run_tasks.py` — 獨立任務腳本，不依賴 Flask/Render，
  直接連 Supabase + LINE API 執行爬取/推播/清理

推播流程先跑一次爬蟲再推播，確保有最新文章。
與 Render 端的 APScheduler 並存不衝突（broadcasted_at 去重機制仍有效）。

**用戶需操作**：到 GitHub repo Settings → Secrets 加入 4 個密鑰。
**推上 GitHub 後即生效**，不需要額外設定。

---

## 待決策：系統穩定性根治（2026-07-25 提出，用戶考慮中）

**背景**：系統已至少三次故障，根因全部相同——
Render 免費方案閒置 15 分鐘休眠，而排程器（APScheduler）綁在會睡著的服務內，
服務一睡排程即死。用 cron-job.org 保活是治標，保活本身也會掛（被停用、超時、配額）。

**核心解法**：把「排程」與「伺服器」拆開。爬蟲/推播不需 24 小時開著的伺服器。

| 方案 | 免費 | 永不休眠 | 互動秒回 | 工作量 | 說明 |
|---|---|---|---|---|---|
| **D. Oracle Cloud Always Free 主機** | ✅ | ✅ | ✅ | 較高(一次性) | 真正永久免費且不休眠的 Linux VM；一次全解，代價是要自管伺服器(更新/安全) |
| A. GitHub Actions 跑排程 | ✅ | ❌(Render 仍睡) | ❌ 首則慢 | 中 | GH 內建排程直接跑爬蟲/推播/清理，直連 Supabase＋LINE，推播不再依賴 Render |
| B. Render 升級付費 | ❌ $7/月 | ✅ | ✅ | 極低 | 服務永不休眠，程式不用改，平台幫你顧維運 |
| C. 維持現狀繼續修 cron-job | ✅ | ❌ | ❌ | 低 | 治標，仍會再掛 |

**用戶偏好**：要「免費 + 一切都穩」。A 犧牲互動(首則要喚醒)、B 要錢——用戶都不滿意。
**最符合用戶需求＝D**（唯一同時滿足 免費＋永不休眠＋互動秒回），代價是設定較複雜
（開 VM、SSH、手動部署），需引導完成，並協助設定基本安全防護（防火牆、更新）。

**若選 D 的實作要點**：Oracle Always Free ARM/AMD VM → 裝 Python → 部署 Flask app →
用 systemd 常駐(取代 Render) → APScheduler 直接可靠運作(不再需要 cron-job 保活) →
LINE Webhook 指向新主機的公開 IP/網域(需設 HTTPS，可用 Caddy/nginx + Let's Encrypt)。
DB 維持 Supabase 不動。

**狀態**：2026-07-25 提出 A/B/D 三方向，用戶傾向 D 但決定「下次再做」，暫緩實作。

---

## 經驗教訓（避免重犯，每次犯錯後補充）

- **URL fallback 反而製造壞連結**：早期 `resolve_url` 把 Google 連結轉成 `/articles/` 格式，
  以為能開，實測是必 400 的死連結。教訓：**改連結格式前，一定先實測目標網址能否開啟**，
  不要憑「看起來合理」就上線。
- **本機正常 ≠ 線上正常**：resolve_url 本機解碼成功但 Render 失敗（疑 IP 被擋）。
  教訓：**涉及外部 API 的功能，要考慮部署環境 IP/網路差異**，並加 log 以便線上診斷。
- **已送出的 LINE 訊息無法追改**：事後修 DB 不會改動已發出的推播。
  教訓：推播類功能，資料要在**發送前**就正確，事後補救只能救「下一次」。
- **驗證要看真實產出**：多次「以為修好」其實沒生效（如摘要只看標題腦補、連結沒真正解碼）。
  教訓：**交付前查實際結果**（查 DB、實際點連結），不憑程式碼看似正確就宣稱完成。
- **破壞性操作前必須先驗證判斷條件**（2026-07-22，實際造成資料損失）：
  用剛寫好、未經完整語料驗證的 `is_scam_article()` 直接對全庫下 DELETE，
  誤刪約 10 篇「防詐宣導」類合格新聞（關鍵字漏了「防詐」；排除詞「貪瀆」
  無條件套用，殺掉「反詐宣導防詐反貪瀆」這類標題）。舊文章 RSS 已無法重爬，
  永久損失。
  **教訓：任何 DELETE 前，先把判斷條件跑過完整語料並人工檢視結果清單，
  確認無誤才執行；能先備份或軟刪除就不要硬刪。**
- **排除詞不可無條件套用**：排除清單應只在「無正向訊號」時才生效，
  否則會誤殺同時含正負詞彙的合格內容。
- **關鍵字比對別碰含 HTML 的欄位**：RSS `description` 內含圖片網址，
  圖檔名 `c8838165.jpg` 讓「165」誤命中，收進完全無關的軍工新聞。
  教訓：**比對前先確認欄位是純文字**；寧可只比對標題（精確），也不要為了
  提高命中率而比對雜訊欄位。
- **單字關鍵字會誤傷**：用「詐」當關鍵字，把「詐領助理費」（公務貪瀆）
  也收進來。教訓：**中文關鍵字要用完整詞彙**，並搭配排除詞清單。
- **安全網修法反而製造新問題**：「推播只挑已解碼文章」本意是避開壞連結，
  卻在「新文章都無法解碼」時導致推播卡在舊文章、天天重複。
  教訓：**加過濾/fallback 前，先想清楚「當條件長期不成立時會怎樣」**，
  避免救 A 問題卻造成 B 問題。根治要從源頭解決（換不需解碼的資料來源）。

---

## 專案現況（快速掌握）

- **狀態**：已上線運作中（Render 部署，DB 已遷至 Supabase，自動排程正常）
- **服務網址**：https://line-scam-bot-bwi2.onrender.com
- **資料庫**：Supabase（Northeast Asia Tokyo，Session pooler 連線，免費無到期限制）
- **GitHub**：https://github.com/carson215079/line-scam-bot（master 分支，push 即自動部署）
- **設計訴求**：好維護、安全

### ⚠️ 待辦事項（依急迫度）

| # | 項目 | 死線 |
|---|---|---|
| 1 | ~~cron-job.org 外部觸發~~ ✅ 2026-08-26 已改用 GitHub Actions 排程（方案A），不再依賴 cron-job.org | ~~已完成~~ |
| 2 | 停用/刪除 Render 舊 PostgreSQL（確認 Supabase 穩定運作數日後）| 2026-07-28 前 |
| 3 | ~~Supabase 遷移~~ ✅ 2026-07-14 完成 | ~~已完成~~ |
| 4 | ~~清理 .env 遷移變數~~ ✅ 2026-07-15 完成 | ~~已完成~~ |
| 5 | ~~Google News 解碼問題~~ ✅ 2026-07-19 已失效（改用台灣媒體 RSS，不再需要解碼）| ~~已解除~~ |

**cron-job.org 需設定的任務**（網址中的 key 換成實際 ADMIN_KEY）：
```
每天 09:00     https://line-scam-bot-bwi2.onrender.com/tasks/broadcast?key=ADMIN_KEY
每 3 小時      https://line-scam-bot-bwi2.onrender.com/tasks/crawl?key=ADMIN_KEY
每 10 分鐘     https://line-scam-bot-bwi2.onrender.com/        （既有保活任務）
```

> 2026-07-22 驗收通過：手動觸發推播 5 則，內容全為真實詐騙案、短連結手機可點、無重複。

---

## 已知風險與未來應對

### 風險：Google News 內部解碼 API 失效
`resolve_url`（crawler/base.py）依賴 Google 非公開的 batchexecute API 解碼真實網址，
Google 改版可能使其失效。**但影響範圍有限**：RSS 本身是公開標準，抓新聞、推播、
摘要都不受影響，僅「連結解碼」退回原始 Google 連結（可點開，需多跳轉一次）。

**未來應對（由簡到繁）：**
1. **Google 只改簽章欄位/參數** → 重抓頁面結構，更新 `resolve_url` 內的正規表達式比對規則（小修）
2. **內部 API 整個關閉** → 改用開源套件如 `googlenewsdecoder`（社群持續維護，跟進 Google 變動）
3. **徹底根治** → 改對接台灣媒體官方 RSS（自由/聯合/TVBS 等），拿到的即真實網址、免解碼。
   當初未採此路是因 165 官網與 ETtoday 有 SSL 憑證問題，但那是個別網站問題，
   多數媒體 RSS 正常，此為長遠最佳方向。

**判斷失效的徵兆**：推播/查詢連結大量變回 `news.google.com/...` 開頭。

> ✅ **上述 Google 解碼風險已於 2026-07-19 解除**：改用台灣媒體 RSS 後
> 不再依賴 Google News，連結本身即為真實短網址。`scripts/fix_urls.py`
> 僅保留供歷史資料修復參考，日常不需使用。

### 新的來源風險（台灣媒體 RSS）
若某媒體改版或 RSS 失效，`RssKeywordCrawler` 會印出「[來源名] 爬取失敗」
並跳過，其他來源照常運作（不會整個停擺）。
**應對**：在 `crawler/base.py` 的 `TAIWAN_FEEDS` 增減來源即可。
已知不可用：中時、三立、公視(SSL)、NOWnews、鏡週刊 — RSS 格式或憑證問題。

## 系統功能總覽

| 功能 | 說明 |
|---|---|
| 關鍵字問答 | 搜 DB + AI 整理；整句搜不到會 AI 萃取關鍵詞重搜；DB 沒資料 fallback AI 知識 |
| 圖片詐騙判斷 | Claude Vision 分析截圖（限私訊）|
| 每日推播 | 每天 09:00 最新 5 篇「未推播過」文章，每篇一則訊息（去重）|
| 自動爬蟲 | 每 3 小時，台灣媒體 RSS（中央社/自由/ETtoday）+ 標題關鍵字過濾 |
| 外部觸發 | `/tasks/crawl`、`/tasks/broadcast`、`/tasks/cleanup`（需 ADMIN_KEY），供 cron-job.org 呼叫 |
| 全文摘要 | 爬蟲抓文章內文給 AI 讀，抓不到 fallback 標題 |
| 多輪對話 | conversations 表存歷史，30 天自動清理（每日 04:00）|
| 群組支援 | 需 @TAG 才回應（比對 bot user_id），群組圖片不處理 |
| 防濫用 | 每人每 24 小時 30 則上限，超額不呼叫 AI |
| 容量保護 | 每日 04:00 檢查 DB 容量，≥400MB 自動刪最舊 20% 文章 |
| 統計指令 | 傳「統計」回文章數/用戶數/最新入庫（台灣時區）/DB 容量 |
| 管理端點 | `/admin/remigrate?key=ADMIN_KEY` 觸發舊資料重整 |

---

## 技術架構

- **Python 3.14 + Flask**，Render 免費方案部署
- **AI**：Claude Haiku（claude-haiku-4-5-20251001），anthropic SDK
- **DB**：PostgreSQL（psycopg3），三張表：articles / users / conversations
- **排程**：APScheduler（Asia/Taipei）— 爬蟲 3hr / 推播 09:00 / 清理 04:00
- **保活**：cron-job.org 每 10 分鐘 ping `/`
- **檔案結構**：main.py（入口）、bot/handler.py（webhook）、ai/summarizer.py（AI）、
  db/database.py（DB）、crawler/（爬蟲）、scheduler/daily_job.py（排程）、
  scripts/remigrate_articles.py（舊資料重整）
- 詳見 `README.md` 與 `整體架構.txt`

---

## 工作歷程（時間序）

### 階段一：建置與部署（2026-06 底 ~ 07 初）
- 完成八大模組開發（DB / AI / 爬蟲 / 排程 / webhook / 入口）
- 部署 Render，串接 LINE Messaging API + Anthropic API
- 排錯：requests 版本衝突、anthropic proxies 錯誤、psycopg2 不相容 Python 3.14（改 psycopg3）
- SQLite → PostgreSQL（Render 重新部署會清空檔案系統）
- 爬蟲來源：165 官網與 ETtoday SSL 失敗 → 改 Google News RSS
- cron-job.org 保活設定（10 分鐘 ping，`/` 健康檢查端點）

### 階段二：體驗優化
- AI 回覆截斷問題：max_tokens 600→2000→改為精簡策略（限 200 字 + max_tokens 500）
- 訊息拆分：段落分配演算法（不硬切句子），最多 3 則（AI 內容 2 則 + 連結 1 則）
- DB 無資料時連結訊息 fallback Google 新聞搜尋頁
- 對話歷史（多輪上下文）
- AI 生成標題 + 自由摘要（不強制「詐騙手法」格式，宣導類文章也能摘要）
- Google News RSS 壞連結修復（resolve_url 轉真實網址 / 網頁版格式）
- 群組支援（@TAG 觸發）、「統計」指令
- 推播移除連結→用戶反悔→恢復（LINE 預覽卡片為平台行為，無法關閉）

### 階段三：全面檢視修正（2026-07-13，Fable 5 檢視）
**六項修正：**
1. 爬蟲對重複文章白呼叫 AI（article_exists 先查再摘要）→ 省 ~150 次/日 API
2. 群組成員 user_id=None 導致 crash（全分支加防護）
3. 單一事件失敗拖垮整批（per-event try/except）
4. ADMIN_KEY 未設定時空字串可通過驗證（空 key 一律拒絕）
5. 統計時間顯示 UTC（SQL 轉 Asia/Taipei）
6. 群組 @TAG 誤判（改比對 bot 自己的 user_id）

**四項改善：**
1. 關鍵字萃取搜尋（整句搜不到時 AI 萃取重搜，短關鍵字不多花錢）
2. 對話 30 天自動清理（每日 04:00）
3. safe_reply（reply token 失效自動改 push 補送）
4. 移除未用套件 beautifulsoup4（後因抓內文功能又加回，現為實際使用）

**三項架構升級：**
1. 防濫用限額（30 則/24hr，文字+圖片計入）
2. 推播每篇一則訊息（原本一則超長訊息）
3. 爬蟲抓文章內文（AI 讀全文寫摘要，取代僅靠標題腦補；
   順序放在重複判斷後，不浪費請求）
4. remigrate 腳本升級：修連結 + 抓內文 + 全文重寫摘要（用戶已觸發執行）

**容量保護（2026-07-13）：**
- DB ≥400MB（80%）時每日清理任務自動刪最舊 20% 文章；只動 articles 不碰 users
- 「統計」指令加顯示 DB 容量，可隨時自行監控
- 估算：全文抓取後每篇約 8KB，500MB 約可存 6 萬篇 ≈ 5 年半，此為保險絲非急救

**資安檢視（2026-07-13）：**
- ✅ 驗證：.env 從未進版控、git 歷史無密鑰、SQL 全參數化、webhook 簽章驗證
- ✅ 修正：ADMIN_KEY 比對改 hmac.compare_digest
- ✅ 用戶已將 ADMIN_KEY 換成長隨機字串
- 📋 已知低風險：prompt injection（bot 無執行權限，影響僅限回覆內容）、
  爬蟲網址來源固定為 Google News RSS（無 SSRF 面）
- ⚠️ 記錄：早期 DB 連線字串曾出現在私人對話中 → Supabase 遷移時自然換新憑證

---

### 階段四：Google News 連結解碼修復（2026-07-14）

**問題**：推播連結持續損壞。實測診斷出根因——
- Google News 不使用 HTTP 轉址（回 200 JS 頁），舊 `resolve_url` 的
  `requests.get(allow_redirects=True)` 從未真正解出網址
- 先前的 fallback（轉 `/articles/` + 去 `?oc=5`）產出的連結 Google 回 400，反而更糟

**修復**（實測 3/3 成功）：
- `resolve_url` 改用 Google 內部 batchexecute API 解碼：
  抓文章頁取 `data-n-a-sg`（簽章）+ `data-n-a-ts`（時間戳）→ POST 解碼 API → 真實網址
- 解碼失敗 fallback 保留原始 RSS 連結（瀏覽器仍可 JS 跳轉），不再轉必壞格式
- 已知風險：依賴 Google 非公開 API，未來可能改版失效；失敗時爬蟲不會停擺
- 除錯備忘：API 回應中引號為跳脫格式（`\"garturlres\"`），比對不可含引號

**同批強化**（防巨型回應攻擊）：
- `fetch_article_text` 改 stream 讀取，上限 2 MB；Content-Type 非 HTML 直接放棄

### 階段五：Supabase 遷移（2026-07-14 完成）

**背景**：Render 免費 PostgreSQL 於 2026-07-28 到期刪庫，須遷移。

**執行**：
- 選 Supabase（免費無期限、內建網頁管理介面），區域 Northeast Asia (Tokyo)
- 安全設定：關閉 Data API + Automatically expose new tables（僅走 PostgreSQL 直連，最小攻擊面）
- 連線用 **Session pooler**（IPv4，Render 相容；Direct connection 為 IPv6 會連不上）
- `scripts/migrate_to_supabase.py`：唯讀來源庫、只寫目標庫，搬完自動筆數核對
- 核對結果：articles 267 / users 2 / conversations 40，全部一致
- 切換 Render 環境變數 DATABASE_URL 指向 Supabase，驗證機器人讀寫正常

**遷移後備忘**：
- Render 舊 DB 先留著數日，確認 Supabase 穩定後再刪
- Supabase 免費方案：500 MB、7 天無活動才暫停（本專案 3hr 爬蟲遠低於門檻，不會暫停）
- 遷移腳本與 .env 的 OLD/NEW_DATABASE_URL 為一次性用途，可清除

## 重要決策紀錄

| 決策 | 原因 |
|---|---|
| Python + Render（非 GAS）| 功能複雜度（AI/圖片/PostgreSQL）超出 GAS 能力 |
| PostgreSQL（非 Notion）| API 速率限制與查詢能力 |
| Google News RSS（非官網直爬）| 165 官網/ETtoday SSL 憑證問題 |
| Claude Haiku | 成本低、速度快（webhook 1 分鐘 reply token 限制）|
| 群組圖片不處理 | 圖片無法 @TAG，避免干擾群組聊天 |
| 費用結構 | 預付制無自動扣款；估 5 人使用約 NT$82/月 |
